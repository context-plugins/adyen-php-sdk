<?php

declare(strict_types=1);

/*
 * Adyen
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

namespace AdyenLib\Models;

use AdyenLib\ApiHelper;
use stdClass;

class PatchableDisputeRequest implements \JsonSerializable
{
    /**
     * @var array
     */
    private $duplicateInfo = [];

    /**
     * @var array
     */
    private $fraudInfo = [];

    /**
     * @var array
     */
    private $notDeliveredInfo = [];

    /**
     * @var array
     */
    private $otherInfo = [];

    /**
     * @var array
     */
    private $status = [];

    /**
     * Returns Duplicate Info.
     * Additional information for raising a dispute of `type` **duplicate**. Required for disputes of
     * `type` **duplicate**.
     */
    public function getDuplicateInfo(): ?PatchableDuplicateInfo
    {
        if (count($this->duplicateInfo) == 0) {
            return null;
        }
        return $this->duplicateInfo['value'];
    }

    /**
     * Sets Duplicate Info.
     * Additional information for raising a dispute of `type` **duplicate**. Required for disputes of
     * `type` **duplicate**.
     *
     * @maps duplicateInfo
     * @mapsBy anyOf(oneOf(PatchableDuplicateInfo),null)
     */
    public function setDuplicateInfo(?PatchableDuplicateInfo $duplicateInfo): void
    {
        $this->duplicateInfo['value'] = $duplicateInfo;
    }

    /**
     * Unsets Duplicate Info.
     * Additional information for raising a dispute of `type` **duplicate**. Required for disputes of
     * `type` **duplicate**.
     */
    public function unsetDuplicateInfo(): void
    {
        $this->duplicateInfo = [];
    }

    /**
     * Returns Fraud Info.
     * Additional information for raising a dispute of `type` **fraud**. Required for disputes of `type`
     * **fraud**.
     */
    public function getFraudInfo(): ?PatchableFraudInfo
    {
        if (count($this->fraudInfo) == 0) {
            return null;
        }
        return $this->fraudInfo['value'];
    }

    /**
     * Sets Fraud Info.
     * Additional information for raising a dispute of `type` **fraud**. Required for disputes of `type`
     * **fraud**.
     *
     * @maps fraudInfo
     * @mapsBy anyOf(oneOf(PatchableFraudInfo),null)
     */
    public function setFraudInfo(?PatchableFraudInfo $fraudInfo): void
    {
        $this->fraudInfo['value'] = $fraudInfo;
    }

    /**
     * Unsets Fraud Info.
     * Additional information for raising a dispute of `type` **fraud**. Required for disputes of `type`
     * **fraud**.
     */
    public function unsetFraudInfo(): void
    {
        $this->fraudInfo = [];
    }

    /**
     * Returns Not Delivered Info.
     * Additional information for raising a dispute of `type` **notDelivered**. Required for disputes of
     * `type` **notDelivered**.
     */
    public function getNotDeliveredInfo(): ?PatchableNotDeliveredInfo
    {
        if (count($this->notDeliveredInfo) == 0) {
            return null;
        }
        return $this->notDeliveredInfo['value'];
    }

    /**
     * Sets Not Delivered Info.
     * Additional information for raising a dispute of `type` **notDelivered**. Required for disputes of
     * `type` **notDelivered**.
     *
     * @maps notDeliveredInfo
     * @mapsBy anyOf(oneOf(PatchableNotDeliveredInfo),null)
     */
    public function setNotDeliveredInfo(?PatchableNotDeliveredInfo $notDeliveredInfo): void
    {
        $this->notDeliveredInfo['value'] = $notDeliveredInfo;
    }

    /**
     * Unsets Not Delivered Info.
     * Additional information for raising a dispute of `type` **notDelivered**. Required for disputes of
     * `type` **notDelivered**.
     */
    public function unsetNotDeliveredInfo(): void
    {
        $this->notDeliveredInfo = [];
    }

    /**
     * Returns Other Info.
     * Additional information for raising a dispute of `type` **other**. Required for disputes of `type`
     * **other**.
     *
     * **Note:** The **other** dispute `type` is currently in beta testing. Do not create or submit any
     * disputes for this dispute `type` at this time.
     */
    public function getOtherInfo(): ?PatchableOtherInfo
    {
        if (count($this->otherInfo) == 0) {
            return null;
        }
        return $this->otherInfo['value'];
    }

    /**
     * Sets Other Info.
     * Additional information for raising a dispute of `type` **other**. Required for disputes of `type`
     * **other**.
     *
     * **Note:** The **other** dispute `type` is currently in beta testing. Do not create or submit any
     * disputes for this dispute `type` at this time.
     *
     * @maps otherInfo
     * @mapsBy anyOf(oneOf(PatchableOtherInfo),null)
     */
    public function setOtherInfo(?PatchableOtherInfo $otherInfo): void
    {
        $this->otherInfo['value'] = $otherInfo;
    }

    /**
     * Unsets Other Info.
     * Additional information for raising a dispute of `type` **other**. Required for disputes of `type`
     * **other**.
     *
     * **Note:** The **other** dispute `type` is currently in beta testing. Do not create or submit any
     * disputes for this dispute `type` at this time.
     */
    public function unsetOtherInfo(): void
    {
        $this->otherInfo = [];
    }

    /**
     * Returns Status.
     * The current status of the dispute.
     *
     * When you create a dispute, you can only set the `status` to **draft**. When you update a dispute,
     * you can set the `status` to **submitted** or **closed**.
     *
     * Possible values: **draft**, **submitted**, **closed**, **won**, **chargeback**,
     * **secondPresentment**.
     */
    public function getStatus(): ?string
    {
        if (count($this->status) == 0) {
            return null;
        }
        return $this->status['value'];
    }

    /**
     * Sets Status.
     * The current status of the dispute.
     *
     * When you create a dispute, you can only set the `status` to **draft**. When you update a dispute,
     * you can set the `status` to **submitted** or **closed**.
     *
     * Possible values: **draft**, **submitted**, **closed**, **won**, **chargeback**,
     * **secondPresentment**.
     *
     * @maps status
     * @mapsBy anyOf(oneOf(DisputeStatusEnum),null)
     * @factory \AdyenLib\Models\DisputeStatusEnum::checkValue DisputeStatusEnum
     */
    public function setStatus(?string $status): void
    {
        $this->status['value'] = $status;
    }

    /**
     * Unsets Status.
     * The current status of the dispute.
     *
     * When you create a dispute, you can only set the `status` to **draft**. When you update a dispute,
     * you can set the `status` to **submitted** or **closed**.
     *
     * Possible values: **draft**, **submitted**, **closed**, **won**, **chargeback**,
     * **secondPresentment**.
     */
    public function unsetStatus(): void
    {
        $this->status = [];
    }

    /**
     * Converts the PatchableDisputeRequest object to a human-readable string representation.
     *
     * @return string The string representation of the PatchableDisputeRequest object.
     */
    public function __toString(): string
    {
        return ApiHelper::stringify(
            'PatchableDisputeRequest',
            [
                'duplicateInfo' => $this->getDuplicateInfo(),
                'fraudInfo' => $this->getFraudInfo(),
                'notDeliveredInfo' => $this->getNotDeliveredInfo(),
                'otherInfo' => $this->getOtherInfo(),
                'status' => $this->getStatus()
            ]
        );
    }

    /**
     * Encode this object to JSON
     *
     * @param bool $asArrayWhenEmpty Whether to serialize this model as an array whenever no fields
     *        are set. (default: false)
     *
     * @return array|stdClass
     */
    #[\ReturnTypeWillChange] // @phan-suppress-current-line PhanUndeclaredClassAttribute for (php < 8.1)
    public function jsonSerialize(bool $asArrayWhenEmpty = false)
    {
        $json = [];
        if (!empty($this->duplicateInfo)) {
            $json['duplicateInfo']    =
                ApiHelper::getJsonHelper()->verifyTypes(
                    $this->duplicateInfo['value'],
                    'anyOf(oneOf(PatchableDuplicateInfo),null)'
                );
        }
        if (!empty($this->fraudInfo)) {
            $json['fraudInfo']        =
                ApiHelper::getJsonHelper()->verifyTypes(
                    $this->fraudInfo['value'],
                    'anyOf(oneOf(PatchableFraudInfo),null)'
                );
        }
        if (!empty($this->notDeliveredInfo)) {
            $json['notDeliveredInfo'] =
                ApiHelper::getJsonHelper()->verifyTypes(
                    $this->notDeliveredInfo['value'],
                    'anyOf(oneOf(PatchableNotDeliveredInfo),null)'
                );
        }
        if (!empty($this->otherInfo)) {
            $json['otherInfo']        =
                ApiHelper::getJsonHelper()->verifyTypes(
                    $this->otherInfo['value'],
                    'anyOf(oneOf(PatchableOtherInfo),null)'
                );
        }
        if (!empty($this->status)) {
            $json['status']           =
                ApiHelper::getJsonHelper()->verifyTypes(
                    $this->status['value'],
                    'anyOf(oneOf(DisputeStatusEnum),null)',
                    [
                        '\AdyenLib\Models\DisputeStatusEnum::checkValue DisputeStatusEnum'
                    ]
                );
        }

        return (!$asArrayWhenEmpty && empty($json)) ? new stdClass() : $json;
    }
}
