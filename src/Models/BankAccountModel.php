<?php

declare(strict_types=1);

/*
 * Adyen
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

namespace AdyenLib\Models;

use AdyenLib\ApiHelper;
use stdClass;

class BankAccountModel implements \JsonSerializable
{
    /**
     * @var array
     */
    private $formFactor = ['value' => FormFactorEnum::PHYSICAL];

    /**
     * Returns Form Factor.
     * Business accounts with a `formFactor` value of **physical** are business accounts issued under the
     * central bank of that country. The default value is **physical** for NL, US, and UK business accounts.
     *
     *
     * Adyen creates a local IBAN for business accounts when the `formFactor` value is set to **virtual**.
     * The local IBANs that are supported are for DE and FR, which reference a physical NL account, with
     * funds being routed through the central bank of NL.
     */
    public function getFormFactor(): ?string
    {
        if (count($this->formFactor) == 0) {
            return null;
        }
        return $this->formFactor['value'];
    }

    /**
     * Sets Form Factor.
     * Business accounts with a `formFactor` value of **physical** are business accounts issued under the
     * central bank of that country. The default value is **physical** for NL, US, and UK business accounts.
     *
     *
     * Adyen creates a local IBAN for business accounts when the `formFactor` value is set to **virtual**.
     * The local IBANs that are supported are for DE and FR, which reference a physical NL account, with
     * funds being routed through the central bank of NL.
     *
     * @maps formFactor
     * @factory \AdyenLib\Models\FormFactorEnum::checkValue
     */
    public function setFormFactor(?string $formFactor): void
    {
        $this->formFactor['value'] = $formFactor;
    }

    /**
     * Unsets Form Factor.
     * Business accounts with a `formFactor` value of **physical** are business accounts issued under the
     * central bank of that country. The default value is **physical** for NL, US, and UK business accounts.
     *
     *
     * Adyen creates a local IBAN for business accounts when the `formFactor` value is set to **virtual**.
     * The local IBANs that are supported are for DE and FR, which reference a physical NL account, with
     * funds being routed through the central bank of NL.
     */
    public function unsetFormFactor(): void
    {
        $this->formFactor = [];
    }

    /**
     * Converts the BankAccountModel object to a human-readable string representation.
     *
     * @return string The string representation of the BankAccountModel object.
     */
    public function __toString(): string
    {
        return ApiHelper::stringify('BankAccountModel', ['formFactor' => $this->getFormFactor()]);
    }

    /**
     * Encode this object to JSON
     *
     * @param bool $asArrayWhenEmpty Whether to serialize this model as an array whenever no fields
     *        are set. (default: false)
     *
     * @return array|stdClass
     */
    #[\ReturnTypeWillChange] // @phan-suppress-current-line PhanUndeclaredClassAttribute for (php < 8.1)
    public function jsonSerialize(bool $asArrayWhenEmpty = false)
    {
        $json = [];
        if (!empty($this->formFactor)) {
            $json['formFactor'] = FormFactorEnum::checkValue($this->formFactor['value']);
        }

        return (!$asArrayWhenEmpty && empty($json)) ? new stdClass() : $json;
    }
}
