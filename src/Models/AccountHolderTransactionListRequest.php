<?php

declare(strict_types=1);

/*
 * Adyen
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

namespace AdyenLib\Models;

use AdyenLib\ApiHelper;
use stdClass;

class AccountHolderTransactionListRequest implements \JsonSerializable
{
    /**
     * @var string
     */
    private $accountHolderCode;

    /**
     * @var TransactionListForAccount[]|null
     */
    private $transactionListsPerAccount;

    /**
     * @var string[]|null
     */
    private $transactionStatuses;

    /**
     * @param string $accountHolderCode
     */
    public function __construct(string $accountHolderCode)
    {
        $this->accountHolderCode = $accountHolderCode;
    }

    /**
     * Returns Account Holder Code.
     * The code of the account holder that owns the account(s) of which retrieve the transaction list.
     */
    public function getAccountHolderCode(): string
    {
        return $this->accountHolderCode;
    }

    /**
     * Sets Account Holder Code.
     * The code of the account holder that owns the account(s) of which retrieve the transaction list.
     *
     * @required
     * @maps accountHolderCode
     */
    public function setAccountHolderCode(string $accountHolderCode): void
    {
        $this->accountHolderCode = $accountHolderCode;
    }

    /**
     * Returns Transaction Lists Per Account.
     * A list of accounts to include in the transaction list. If left blank, the last fifty (50)
     * transactions for all accounts of the account holder will be included.
     *
     * @return TransactionListForAccount[]|null
     */
    public function getTransactionListsPerAccount(): ?array
    {
        return $this->transactionListsPerAccount;
    }

    /**
     * Sets Transaction Lists Per Account.
     * A list of accounts to include in the transaction list. If left blank, the last fifty (50)
     * transactions for all accounts of the account holder will be included.
     *
     * @maps transactionListsPerAccount
     *
     * @param TransactionListForAccount[]|null $transactionListsPerAccount
     */
    public function setTransactionListsPerAccount(?array $transactionListsPerAccount): void
    {
        $this->transactionListsPerAccount = $transactionListsPerAccount;
    }

    /**
     * Returns Transaction Statuses.
     * A list of statuses to include in the transaction list. If left blank, all transactions will be
     * included.
     * >Permitted values:
     * >* `PendingCredit` - a pending balance credit.
     * >* `CreditFailed` - a pending credit failure; the balance will not be credited.
     * >* `Credited` - a credited balance.
     * >* `PendingDebit` - a pending balance debit (e.g., a refund).
     * >* `CreditClosed` - a pending credit closed; the balance will not be credited.
     * >* `CreditSuspended` - a pending credit closed; the balance will not be credited.
     * >* `DebitFailed` - a pending debit failure; the balance will not be debited.
     * >* `Debited` - a debited balance (e.g., a refund).
     * >* `DebitReversedReceived` - a pending refund reversal.
     * >* `DebitedReversed` - a reversed refund.
     * >* `ChargebackReceived` - a received chargeback request.
     * >* `Chargeback` - a processed chargeback.
     * >* `ChargebackReversedReceived` - a pending chargeback reversal.
     * >* `ChargebackReversed` - a reversed chargeback.
     * >* `Converted` - converted.
     * >* `ManualCorrected` - manual booking/adjustment by Adyen.
     * >* `Payout` - a payout.
     * >* `PayoutReversed` - a reversed payout.
     * >* `PendingFundTransfer` - a pending transfer of funds from one account to another.
     * >* `FundTransfer` - a transfer of funds from one account to another.
     *
     * @return string[]|null
     */
    public function getTransactionStatuses(): ?array
    {
        return $this->transactionStatuses;
    }

    /**
     * Sets Transaction Statuses.
     * A list of statuses to include in the transaction list. If left blank, all transactions will be
     * included.
     * >Permitted values:
     * >* `PendingCredit` - a pending balance credit.
     * >* `CreditFailed` - a pending credit failure; the balance will not be credited.
     * >* `Credited` - a credited balance.
     * >* `PendingDebit` - a pending balance debit (e.g., a refund).
     * >* `CreditClosed` - a pending credit closed; the balance will not be credited.
     * >* `CreditSuspended` - a pending credit closed; the balance will not be credited.
     * >* `DebitFailed` - a pending debit failure; the balance will not be debited.
     * >* `Debited` - a debited balance (e.g., a refund).
     * >* `DebitReversedReceived` - a pending refund reversal.
     * >* `DebitedReversed` - a reversed refund.
     * >* `ChargebackReceived` - a received chargeback request.
     * >* `Chargeback` - a processed chargeback.
     * >* `ChargebackReversedReceived` - a pending chargeback reversal.
     * >* `ChargebackReversed` - a reversed chargeback.
     * >* `Converted` - converted.
     * >* `ManualCorrected` - manual booking/adjustment by Adyen.
     * >* `Payout` - a payout.
     * >* `PayoutReversed` - a reversed payout.
     * >* `PendingFundTransfer` - a pending transfer of funds from one account to another.
     * >* `FundTransfer` - a transfer of funds from one account to another.
     *
     * @maps transactionStatuses
     * @factory \AdyenLib\Models\TransactionStatusEnum::checkValue
     *
     * @param string[]|null $transactionStatuses
     */
    public function setTransactionStatuses(?array $transactionStatuses): void
    {
        $this->transactionStatuses = $transactionStatuses;
    }

    /**
     * Converts the AccountHolderTransactionListRequest object to a human-readable string representation.
     *
     * @return string The string representation of the AccountHolderTransactionListRequest object.
     */
    public function __toString(): string
    {
        return ApiHelper::stringify(
            'AccountHolderTransactionListRequest',
            [
                'accountHolderCode' => $this->accountHolderCode,
                'transactionListsPerAccount' => $this->transactionListsPerAccount,
                'transactionStatuses' => $this->transactionStatuses
            ]
        );
    }

    /**
     * Encode this object to JSON
     *
     * @param bool $asArrayWhenEmpty Whether to serialize this model as an array whenever no fields
     *        are set. (default: false)
     *
     * @return array|stdClass
     */
    #[\ReturnTypeWillChange] // @phan-suppress-current-line PhanUndeclaredClassAttribute for (php < 8.1)
    public function jsonSerialize(bool $asArrayWhenEmpty = false)
    {
        $json = [];
        $json['accountHolderCode']              = $this->accountHolderCode;
        if (isset($this->transactionListsPerAccount)) {
            $json['transactionListsPerAccount'] = $this->transactionListsPerAccount;
        }
        if (isset($this->transactionStatuses)) {
            $json['transactionStatuses']        = TransactionStatusEnum::checkValue($this->transactionStatuses);
        }

        return (!$asArrayWhenEmpty && empty($json)) ? new stdClass() : $json;
    }
}
