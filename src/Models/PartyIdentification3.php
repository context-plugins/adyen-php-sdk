<?php

declare(strict_types=1);

/*
 * Adyen
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

namespace AdyenLib\Models;

use AdyenLib\ApiHelper;
use AdyenLib\Utils\DateTimeHelper;
use stdClass;

/**
 * Information about the owner of the bank account.
 */
class PartyIdentification3 implements \JsonSerializable
{
    /**
     * @var Address12|null
     */
    private $address;

    /**
     * @var \DateTime|null
     */
    private $dateOfBirth;

    /**
     * @var string|null
     */
    private $email;

    /**
     * @var string|null
     */
    private $firstName;

    /**
     * @var string|null
     */
    private $fullName;

    /**
     * @var string|null
     */
    private $lastName;

    /**
     * @var string|null
     */
    private $reference;

    /**
     * @var string|null
     */
    private $type = Type112Enum::UNKNOWN;

    /**
     * @var string|null
     */
    private $url;

    /**
     * Returns Address.
     * The address of the bank account or card owner.
     */
    public function getAddress(): ?Address12
    {
        return $this->address;
    }

    /**
     * Sets Address.
     * The address of the bank account or card owner.
     *
     * @maps address
     */
    public function setAddress(?Address12 $address): void
    {
        $this->address = $address;
    }

    /**
     * Returns Date of Birth.
     * The date of birth of the individual in [ISO-8601](https://www.w3.org/TR/NOTE-datetime) format. For
     * example, **YYYY-MM-DD**.
     *
     * Allowed only when `type` is **individual**.
     */
    public function getDateOfBirth(): ?\DateTime
    {
        return $this->dateOfBirth;
    }

    /**
     * Sets Date of Birth.
     * The date of birth of the individual in [ISO-8601](https://www.w3.org/TR/NOTE-datetime) format. For
     * example, **YYYY-MM-DD**.
     *
     * Allowed only when `type` is **individual**.
     *
     * @maps dateOfBirth
     * @factory \AdyenLib\Utils\DateTimeHelper::fromSimpleDate
     */
    public function setDateOfBirth(?\DateTime $dateOfBirth): void
    {
        $this->dateOfBirth = $dateOfBirth;
    }

    /**
     * Returns Email.
     * The email address of the organization or individual. Maximum length: 254 characters.
     */
    public function getEmail(): ?string
    {
        return $this->email;
    }

    /**
     * Sets Email.
     * The email address of the organization or individual. Maximum length: 254 characters.
     *
     * @maps email
     */
    public function setEmail(?string $email): void
    {
        $this->email = $email;
    }

    /**
     * Returns First Name.
     * The first name of the individual.
     *
     * Supported characters: [a-z] [A-Z] - . / — and space.
     *
     * This parameter is:
     * - Allowed only when `type` is **individual**.
     * - Required when `category` is **card**.
     */
    public function getFirstName(): ?string
    {
        return $this->firstName;
    }

    /**
     * Sets First Name.
     * The first name of the individual.
     *
     * Supported characters: [a-z] [A-Z] - . / — and space.
     *
     * This parameter is:
     * - Allowed only when `type` is **individual**.
     * - Required when `category` is **card**.
     *
     * @maps firstName
     */
    public function setFirstName(?string $firstName): void
    {
        $this->firstName = $firstName;
    }

    /**
     * Returns Full Name.
     * The full name of the entity that owns the bank account or card.
     *
     * Supported characters: [a-z] [A-Z] [0-9] , . ; : - — / \ + & ! ? @ ( ) " ' and space.
     *
     * Required when `category` is **bank**.
     */
    public function getFullName(): ?string
    {
        return $this->fullName;
    }

    /**
     * Sets Full Name.
     * The full name of the entity that owns the bank account or card.
     *
     * Supported characters: [a-z] [A-Z] [0-9] , . ; : - — / \ + & ! ? @ ( ) " ' and space.
     *
     * Required when `category` is **bank**.
     *
     * @maps fullName
     */
    public function setFullName(?string $fullName): void
    {
        $this->fullName = $fullName;
    }

    /**
     * Returns Last Name.
     * The last name of the individual.
     *
     * Supported characters: [a-z] [A-Z] - . / — and space.
     *
     * This parameter is:
     * - Allowed only when `type` is **individual**.
     * - Required when `category` is **card**.
     */
    public function getLastName(): ?string
    {
        return $this->lastName;
    }

    /**
     * Sets Last Name.
     * The last name of the individual.
     *
     * Supported characters: [a-z] [A-Z] - . / — and space.
     *
     * This parameter is:
     * - Allowed only when `type` is **individual**.
     * - Required when `category` is **card**.
     *
     * @maps lastName
     */
    public function setLastName(?string $lastName): void
    {
        $this->lastName = $lastName;
    }

    /**
     * Returns Reference.
     * A unique reference to identify the party or counterparty involved in the transfer. For example, your
     * client's unique wallet or payee ID.
     *
     * Required when you include `cardIdentification.storedPaymentMethodId`.
     */
    public function getReference(): ?string
    {
        return $this->reference;
    }

    /**
     * Sets Reference.
     * A unique reference to identify the party or counterparty involved in the transfer. For example, your
     * client's unique wallet or payee ID.
     *
     * Required when you include `cardIdentification.storedPaymentMethodId`.
     *
     * @maps reference
     */
    public function setReference(?string $reference): void
    {
        $this->reference = $reference;
    }

    /**
     * Returns Type.
     * The type of entity that owns the bank account or card.
     *
     * Possible values: **individual**, **organization**, or **unknown**.
     *
     * Required when `category` is **card**. In this case, the value must be **individual**.
     */
    public function getType(): ?string
    {
        return $this->type;
    }

    /**
     * Sets Type.
     * The type of entity that owns the bank account or card.
     *
     * Possible values: **individual**, **organization**, or **unknown**.
     *
     * Required when `category` is **card**. In this case, the value must be **individual**.
     *
     * @maps type
     * @factory \AdyenLib\Models\Type112Enum::checkValue
     */
    public function setType(?string $type): void
    {
        $this->type = $type;
    }

    /**
     * Returns Url.
     * The URL of the organization or individual. Maximum length: 255 characters.
     */
    public function getUrl(): ?string
    {
        return $this->url;
    }

    /**
     * Sets Url.
     * The URL of the organization or individual. Maximum length: 255 characters.
     *
     * @maps url
     */
    public function setUrl(?string $url): void
    {
        $this->url = $url;
    }

    /**
     * Converts the PartyIdentification3 object to a human-readable string representation.
     *
     * @return string The string representation of the PartyIdentification3 object.
     */
    public function __toString(): string
    {
        return ApiHelper::stringify(
            'PartyIdentification3',
            [
                'address' => $this->address,
                'dateOfBirth' => $this->dateOfBirth,
                'email' => $this->email,
                'firstName' => $this->firstName,
                'fullName' => $this->fullName,
                'lastName' => $this->lastName,
                'reference' => $this->reference,
                'type' => $this->type,
                'url' => $this->url
            ]
        );
    }

    /**
     * Encode this object to JSON
     *
     * @param bool $asArrayWhenEmpty Whether to serialize this model as an array whenever no fields
     *        are set. (default: false)
     *
     * @return array|stdClass
     */
    #[\ReturnTypeWillChange] // @phan-suppress-current-line PhanUndeclaredClassAttribute for (php < 8.1)
    public function jsonSerialize(bool $asArrayWhenEmpty = false)
    {
        $json = [];
        if (isset($this->address)) {
            $json['address']     = $this->address;
        }
        if (isset($this->dateOfBirth)) {
            $json['dateOfBirth'] = DateTimeHelper::toSimpleDate($this->dateOfBirth);
        }
        if (isset($this->email)) {
            $json['email']       = $this->email;
        }
        if (isset($this->firstName)) {
            $json['firstName']   = $this->firstName;
        }
        if (isset($this->fullName)) {
            $json['fullName']    = $this->fullName;
        }
        if (isset($this->lastName)) {
            $json['lastName']    = $this->lastName;
        }
        if (isset($this->reference)) {
            $json['reference']   = $this->reference;
        }
        if (isset($this->type)) {
            $json['type']        = Type112Enum::checkValue($this->type);
        }
        if (isset($this->url)) {
            $json['url']         = $this->url;
        }

        return (!$asArrayWhenEmpty && empty($json)) ? new stdClass() : $json;
    }
}
