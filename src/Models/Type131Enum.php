<?php

declare(strict_types=1);

/*
 * Adyen
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

namespace AdyenLib\Models;

use Core\Utils\CoreHelper;
use Exception;
use stdClass;

/**
 * The [type of interval](https://docs.adyen.com/issuing/transaction-rules#time-intervals) during which
 * the rule conditions and limits apply, and how often counters are reset.
 *
 * Possible values:
 * * **perTransaction**: conditions are evaluated and the counters are reset for every transaction.
 * * **daily**: the counters are reset daily at 00:00:00 CET.
 * * **weekly**: the counters are reset every Monday at 00:00:00 CET.
 * * **monthly**: the counters reset every first day of the month at 00:00:00 CET.
 * * **lifetime**: conditions are applied to the lifetime of the payment instrument.
 * * **rolling**: conditions are applied and the counters are reset based on a `duration`. If the
 * reset date and time are not provided, Adyen applies the default reset time similar to fixed
 * intervals. For example, if the duration is every two weeks, the counter resets every third Monday at
 * 00:00:00 CET.
 * * **sliding**: conditions are applied and the counters are reset based on the current time and a
 * `duration` that you specify.
 */
class Type131Enum
{
    public const DAILY = 'daily';

    public const LIFETIME = 'lifetime';

    public const MONTHLY = 'monthly';

    public const PERTRANSACTION = 'perTransaction';

    public const ROLLING = 'rolling';

    public const SLIDING = 'sliding';

    public const WEEKLY = 'weekly';

    private const _ALL_VALUES =
        [
            self::DAILY,
            self::LIFETIME,
            self::MONTHLY,
            self::PERTRANSACTION,
            self::ROLLING,
            self::SLIDING,
            self::WEEKLY
        ];

    /**
     * Ensures that all the given values are present in this Enum.
     *
     * @param array|stdClass|null|string $value Value or a list/map of values to be checked
     *
     * @return array|null|string Input value(s), if all are a part of this Enum
     *
     * @throws Exception Throws exception if any given value is not in this Enum
     */
    public static function checkValue($value)
    {
        $value = json_decode(json_encode($value), true); // converts stdClass into array
        if (CoreHelper::checkValueOrValuesInList($value, self::_ALL_VALUES)) {
            return $value;
        }
        throw new Exception("$value is invalid for Type131Enum.");
    }
}
