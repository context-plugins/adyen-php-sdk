
# Fraud Result 2

The fraud result properties of the payment.

## Structure

`FraudResult2`

## Fields

| Name | Type | Tags | Description | Getter | Setter |
|  --- | --- | --- | --- | --- | --- |
| `accountScore` | `int` | Required | The total fraud score generated by the risk checks. | getAccountScore(): int | setAccountScore(int accountScore): void |
| `results` | [`?(FraudCheckResultWrapper[])`](../../doc/models/fraud-check-result-wrapper.md) | Optional | The result of the individual risk checks. | getResults(): ?array | setResults(?array results): void |

## Example

```php
use AdyenLib\Models\Builders\FraudResult2Builder;
use AdyenLib\Models\Builders\FraudCheckResultWrapperBuilder;
use AdyenLib\Models\Builders\FraudCheckResultBuilder;

$fraudResult2 = FraudResult2Builder::init(
    62
)
    ->results(
        [
            FraudCheckResultWrapperBuilder::init()
                ->fraudCheckResult(
                    FraudCheckResultBuilder::init(
                        114,
                        2,
                        'name0'
                    )->build()
                )->build()
        ]
    )->build();
```

