<?php

declare(strict_types=1);

/*
 * Adyen
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

namespace AdyenLib\Models;

use AdyenLib\ApiHelper;
use stdClass;

class TransactionRuleInterval implements \JsonSerializable
{
    /**
     * @var int|null
     */
    private $dayOfMonth;

    /**
     * @var string|null
     */
    private $dayOfWeek;

    /**
     * @var Duration1|null
     */
    private $duration;

    /**
     * @var string|null
     */
    private $timeOfDay;

    /**
     * @var string|null
     */
    private $timeZone;

    /**
     * @var string
     */
    private $type;

    /**
     * @param string $type
     */
    public function __construct(string $type)
    {
        $this->type = $type;
    }

    /**
     * Returns Day of Month.
     * The day of month, used when the `duration.unit` is **months**. If not provided, by default, this is
     * set to **1**, the first day of the month.
     */
    public function getDayOfMonth(): ?int
    {
        return $this->dayOfMonth;
    }

    /**
     * Sets Day of Month.
     * The day of month, used when the `duration.unit` is **months**. If not provided, by default, this is
     * set to **1**, the first day of the month.
     *
     * @maps dayOfMonth
     */
    public function setDayOfMonth(?int $dayOfMonth): void
    {
        $this->dayOfMonth = $dayOfMonth;
    }

    /**
     * Returns Day of Week.
     * The day of week, used when the `duration.unit` is **weeks**. If not provided, by default, this is
     * set to **monday**.
     *
     * Possible values: **sunday**, **monday**, **tuesday**, **wednesday**, **thursday**, **friday**.
     */
    public function getDayOfWeek(): ?string
    {
        return $this->dayOfWeek;
    }

    /**
     * Sets Day of Week.
     * The day of week, used when the `duration.unit` is **weeks**. If not provided, by default, this is
     * set to **monday**.
     *
     * Possible values: **sunday**, **monday**, **tuesday**, **wednesday**, **thursday**, **friday**.
     *
     * @maps dayOfWeek
     * @factory \AdyenLib\Models\DayOfWeekEnum::checkValue
     */
    public function setDayOfWeek(?string $dayOfWeek): void
    {
        $this->dayOfWeek = $dayOfWeek;
    }

    /**
     * Returns Duration.
     * The duration, which you can specify in hours, days, weeks, or months. The maximum duration is 90
     * days or an equivalent in other units. Required when the `type` is **rolling** or **sliding**.
     */
    public function getDuration(): ?Duration1
    {
        return $this->duration;
    }

    /**
     * Sets Duration.
     * The duration, which you can specify in hours, days, weeks, or months. The maximum duration is 90
     * days or an equivalent in other units. Required when the `type` is **rolling** or **sliding**.
     *
     * @maps duration
     */
    public function setDuration(?Duration1 $duration): void
    {
        $this->duration = $duration;
    }

    /**
     * Returns Time of Day.
     * The time of day, in **hh:mm:ss** format, used when the `duration.unit` is **hours**. If not provided,
     * by default, this is set to **00:00:00**.
     */
    public function getTimeOfDay(): ?string
    {
        return $this->timeOfDay;
    }

    /**
     * Sets Time of Day.
     * The time of day, in **hh:mm:ss** format, used when the `duration.unit` is **hours**. If not provided,
     * by default, this is set to **00:00:00**.
     *
     * @maps timeOfDay
     */
    public function setTimeOfDay(?string $timeOfDay): void
    {
        $this->timeOfDay = $timeOfDay;
    }

    /**
     * Returns Time Zone.
     * The [time zone](https://en.wikipedia.org/wiki/List_of_tz_database_time_zones). For example,
     * **Europe/Amsterdam**. By default, this is set to **UTC**.
     */
    public function getTimeZone(): ?string
    {
        return $this->timeZone;
    }

    /**
     * Sets Time Zone.
     * The [time zone](https://en.wikipedia.org/wiki/List_of_tz_database_time_zones). For example,
     * **Europe/Amsterdam**. By default, this is set to **UTC**.
     *
     * @maps timeZone
     */
    public function setTimeZone(?string $timeZone): void
    {
        $this->timeZone = $timeZone;
    }

    /**
     * Returns Type.
     * The [type of interval](https://docs.adyen.com/issuing/transaction-rules#time-intervals) during which
     * the rule conditions and limits apply, and how often counters are reset.
     *
     * Possible values:
     * * **perTransaction**: conditions are evaluated and the counters are reset for every transaction.
     * * **daily**: the counters are reset daily at 00:00:00 CET.
     * * **weekly**: the counters are reset every Monday at 00:00:00 CET.
     * * **monthly**: the counters reset every first day of the month at 00:00:00 CET.
     * * **lifetime**: conditions are applied to the lifetime of the payment instrument.
     * * **rolling**: conditions are applied and the counters are reset based on a `duration`. If the
     * reset date and time are not provided, Adyen applies the default reset time similar to fixed
     * intervals. For example, if the duration is every two weeks, the counter resets every third Monday at
     * 00:00:00 CET.
     * * **sliding**: conditions are applied and the counters are reset based on the current time and a
     * `duration` that you specify.
     */
    public function getType(): string
    {
        return $this->type;
    }

    /**
     * Sets Type.
     * The [type of interval](https://docs.adyen.com/issuing/transaction-rules#time-intervals) during which
     * the rule conditions and limits apply, and how often counters are reset.
     *
     * Possible values:
     * * **perTransaction**: conditions are evaluated and the counters are reset for every transaction.
     * * **daily**: the counters are reset daily at 00:00:00 CET.
     * * **weekly**: the counters are reset every Monday at 00:00:00 CET.
     * * **monthly**: the counters reset every first day of the month at 00:00:00 CET.
     * * **lifetime**: conditions are applied to the lifetime of the payment instrument.
     * * **rolling**: conditions are applied and the counters are reset based on a `duration`. If the
     * reset date and time are not provided, Adyen applies the default reset time similar to fixed
     * intervals. For example, if the duration is every two weeks, the counter resets every third Monday at
     * 00:00:00 CET.
     * * **sliding**: conditions are applied and the counters are reset based on the current time and a
     * `duration` that you specify.
     *
     * @required
     * @maps type
     * @factory \AdyenLib\Models\Type131Enum::checkValue
     */
    public function setType(string $type): void
    {
        $this->type = $type;
    }

    /**
     * Converts the TransactionRuleInterval object to a human-readable string representation.
     *
     * @return string The string representation of the TransactionRuleInterval object.
     */
    public function __toString(): string
    {
        return ApiHelper::stringify(
            'TransactionRuleInterval',
            [
                'dayOfMonth' => $this->dayOfMonth,
                'dayOfWeek' => $this->dayOfWeek,
                'duration' => $this->duration,
                'timeOfDay' => $this->timeOfDay,
                'timeZone' => $this->timeZone,
                'type' => $this->type
            ]
        );
    }

    /**
     * Encode this object to JSON
     *
     * @param bool $asArrayWhenEmpty Whether to serialize this model as an array whenever no fields
     *        are set. (default: false)
     *
     * @return array|stdClass
     */
    #[\ReturnTypeWillChange] // @phan-suppress-current-line PhanUndeclaredClassAttribute for (php < 8.1)
    public function jsonSerialize(bool $asArrayWhenEmpty = false)
    {
        $json = [];
        if (isset($this->dayOfMonth)) {
            $json['dayOfMonth'] = $this->dayOfMonth;
        }
        if (isset($this->dayOfWeek)) {
            $json['dayOfWeek']  = DayOfWeekEnum::checkValue($this->dayOfWeek);
        }
        if (isset($this->duration)) {
            $json['duration']   = $this->duration;
        }
        if (isset($this->timeOfDay)) {
            $json['timeOfDay']  = $this->timeOfDay;
        }
        if (isset($this->timeZone)) {
            $json['timeZone']   = $this->timeZone;
        }
        $json['type']           = Type131Enum::checkValue($this->type);

        return (!$asArrayWhenEmpty && empty($json)) ? new stdClass() : $json;
    }
}
