<?php

declare(strict_types=1);

/*
 * Adyen
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

namespace AdyenLib\Models;

use AdyenLib\ApiHelper;
use stdClass;

class SweepSchedule implements \JsonSerializable
{
    /**
     * @var string|null
     */
    private $cronExpression;

    /**
     * @var string
     */
    private $type;

    /**
     * @param string $type
     */
    public function __construct(string $type)
    {
        $this->type = $type;
    }

    /**
     * Returns Cron Expression.
     * A [cron expression](https://en.wikipedia.org/wiki/Cron#CRON_expression) that is used to set the
     * sweep schedule. The schedule uses the time zone of the balance account.
     * For example, **30 17 * * MON** schedules a sweep every Monday at 17:30.
     *
     * The expression must have five values separated by a single space in the following order:
     *
     * * Minute: **0-59**
     *
     * * Hour: **0-23**
     *
     * * Day of the month: **1-31**
     *
     * * Month: **1-12** or **JAN-DEC**
     *
     * * Day of the week: **0-7** (0 and 7 are Sunday) or **MON-SUN**.
     *
     * The following non-standard characters are supported: **&ast;**, **L**, **#**, **W** and ** /**. See
     * [crontab guru](https://crontab.guru/) for more examples.
     *
     * Required when `type` is **cron**.
     */
    public function getCronExpression(): ?string
    {
        return $this->cronExpression;
    }

    /**
     * Sets Cron Expression.
     * A [cron expression](https://en.wikipedia.org/wiki/Cron#CRON_expression) that is used to set the
     * sweep schedule. The schedule uses the time zone of the balance account.
     * For example, **30 17 * * MON** schedules a sweep every Monday at 17:30.
     *
     * The expression must have five values separated by a single space in the following order:
     *
     * * Minute: **0-59**
     *
     * * Hour: **0-23**
     *
     * * Day of the month: **1-31**
     *
     * * Month: **1-12** or **JAN-DEC**
     *
     * * Day of the week: **0-7** (0 and 7 are Sunday) or **MON-SUN**.
     *
     * The following non-standard characters are supported: **&ast;**, **L**, **#**, **W** and ** /**. See
     * [crontab guru](https://crontab.guru/) for more examples.
     *
     * Required when `type` is **cron**.
     *
     * @maps cronExpression
     */
    public function setCronExpression(?string $cronExpression): void
    {
        $this->cronExpression = $cronExpression;
    }

    /**
     * Returns Type.
     * The schedule type.
     *
     * Possible values:
     *
     * * **cron**: push out funds based on a `cronExpression`.
     *
     * * **daily**: push out funds daily at 07:00 AM CET.
     *
     * * **weekly**: push out funds every Monday at 07:00 AM CET.
     *
     * * **monthly**: push out funds every first of the month at 07:00 AM CET.
     *
     * * **balance**: execute the sweep instantly if the `triggerAmount` is reached.
     */
    public function getType(): string
    {
        return $this->type;
    }

    /**
     * Sets Type.
     * The schedule type.
     *
     * Possible values:
     *
     * * **cron**: push out funds based on a `cronExpression`.
     *
     * * **daily**: push out funds daily at 07:00 AM CET.
     *
     * * **weekly**: push out funds every Monday at 07:00 AM CET.
     *
     * * **monthly**: push out funds every first of the month at 07:00 AM CET.
     *
     * * **balance**: execute the sweep instantly if the `triggerAmount` is reached.
     *
     * @required
     * @maps type
     * @factory \AdyenLib\Models\Type62Enum::checkValue
     */
    public function setType(string $type): void
    {
        $this->type = $type;
    }

    /**
     * Converts the SweepSchedule object to a human-readable string representation.
     *
     * @return string The string representation of the SweepSchedule object.
     */
    public function __toString(): string
    {
        return ApiHelper::stringify(
            'SweepSchedule',
            ['cronExpression' => $this->cronExpression, 'type' => $this->type]
        );
    }

    /**
     * Encode this object to JSON
     *
     * @param bool $asArrayWhenEmpty Whether to serialize this model as an array whenever no fields
     *        are set. (default: false)
     *
     * @return array|stdClass
     */
    #[\ReturnTypeWillChange] // @phan-suppress-current-line PhanUndeclaredClassAttribute for (php < 8.1)
    public function jsonSerialize(bool $asArrayWhenEmpty = false)
    {
        $json = [];
        if (isset($this->cronExpression)) {
            $json['cronExpression'] = $this->cronExpression;
        }
        $json['type']               = Type62Enum::checkValue($this->type);

        return (!$asArrayWhenEmpty && empty($json)) ? new stdClass() : $json;
    }
}
