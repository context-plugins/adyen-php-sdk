<?php

declare(strict_types=1);

/*
 * Adyen
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

namespace AdyenLib\Models;

use AdyenLib\ApiHelper;
use stdClass;

class CapturedSignature implements \JsonSerializable
{
    /**
     * @var AreaSize1|null
     */
    private $areaSize;

    /**
     * @var Point[]|null
     */
    private $signaturePoint;

    /**
     * Returns Area Size.
     * Size of an area. Contain the size of the pad area where the signature is written, given with the
     * maximum abscissa and ordinate values (X and Y). The maximum value is FFFF.
     */
    public function getAreaSize(): ?AreaSize1
    {
        return $this->areaSize;
    }

    /**
     * Sets Area Size.
     * Size of an area. Contain the size of the pad area where the signature is written, given with the
     * maximum abscissa and ordinate values (X and Y). The maximum value is FFFF.
     *
     * @maps AreaSize
     */
    public function setAreaSize(?AreaSize1 $areaSize): void
    {
        $this->areaSize = $areaSize;
    }

    /**
     * Returns Signature Point.
     * Coordinates of a point where the pen changes direction or lift. Contain the Coordinates of a point
     * of the written signature where the pen changes direction or lift where (X and Y). When the signer
     * lifts the pen, both X and Y have the value FFFF.
     *
     * @return Point[]|null
     */
    public function getSignaturePoint(): ?array
    {
        return $this->signaturePoint;
    }

    /**
     * Sets Signature Point.
     * Coordinates of a point where the pen changes direction or lift. Contain the Coordinates of a point
     * of the written signature where the pen changes direction or lift where (X and Y). When the signer
     * lifts the pen, both X and Y have the value FFFF.
     *
     * @maps SignaturePoint
     *
     * @param Point[]|null $signaturePoint
     */
    public function setSignaturePoint(?array $signaturePoint): void
    {
        $this->signaturePoint = $signaturePoint;
    }

    /**
     * Converts the CapturedSignature object to a human-readable string representation.
     *
     * @return string The string representation of the CapturedSignature object.
     */
    public function __toString(): string
    {
        return ApiHelper::stringify(
            'CapturedSignature',
            ['areaSize' => $this->areaSize, 'signaturePoint' => $this->signaturePoint]
        );
    }

    /**
     * Encode this object to JSON
     *
     * @param bool $asArrayWhenEmpty Whether to serialize this model as an array whenever no fields
     *        are set. (default: false)
     *
     * @return array|stdClass
     */
    #[\ReturnTypeWillChange] // @phan-suppress-current-line PhanUndeclaredClassAttribute for (php < 8.1)
    public function jsonSerialize(bool $asArrayWhenEmpty = false)
    {
        $json = [];
        if (isset($this->areaSize)) {
            $json['AreaSize']       = $this->areaSize;
        }
        if (isset($this->signaturePoint)) {
            $json['SignaturePoint'] = $this->signaturePoint;
        }

        return (!$asArrayWhenEmpty && empty($json)) ? new stdClass() : $json;
    }
}
