<?php

declare(strict_types=1);

/*
 * Adyen
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

namespace AdyenLib\Models;

use AdyenLib\ApiHelper;
use stdClass;

class FindTerminalResponse implements \JsonSerializable
{
    /**
     * @var string
     */
    private $companyAccount;

    /**
     * @var string|null
     */
    private $merchantAccount;

    /**
     * @var bool|null
     */
    private $merchantInventory;

    /**
     * @var string|null
     */
    private $store;

    /**
     * @var string
     */
    private $terminal;

    /**
     * @param string $companyAccount
     * @param string $terminal
     */
    public function __construct(string $companyAccount, string $terminal)
    {
        $this->companyAccount = $companyAccount;
        $this->terminal = $terminal;
    }

    /**
     * Returns Company Account.
     * The company account that the terminal is associated with. If this is the only account level shown in
     * the response, the terminal is assigned to the inventory of the company account.
     */
    public function getCompanyAccount(): string
    {
        return $this->companyAccount;
    }

    /**
     * Sets Company Account.
     * The company account that the terminal is associated with. If this is the only account level shown in
     * the response, the terminal is assigned to the inventory of the company account.
     *
     * @required
     * @maps companyAccount
     */
    public function setCompanyAccount(string $companyAccount): void
    {
        $this->companyAccount = $companyAccount;
    }

    /**
     * Returns Merchant Account.
     * The merchant account that the terminal is associated with. If the response doesn't contain a `store`
     * the terminal is assigned to this merchant account.
     */
    public function getMerchantAccount(): ?string
    {
        return $this->merchantAccount;
    }

    /**
     * Sets Merchant Account.
     * The merchant account that the terminal is associated with. If the response doesn't contain a `store`
     * the terminal is assigned to this merchant account.
     *
     * @maps merchantAccount
     */
    public function setMerchantAccount(?string $merchantAccount): void
    {
        $this->merchantAccount = $merchantAccount;
    }

    /**
     * Returns Merchant Inventory.
     * Boolean that indicates if the terminal is assigned to the merchant inventory. This is returned when
     * the terminal is assigned to a merchant account.
     *
     * - If **true**, this indicates that the terminal is in the merchant inventory. This also means that
     * the terminal cannot be boarded.
     *
     * - If **false**, this indicates that the terminal is assigned to the merchant account as an in-store
     * terminal. This means that the terminal is ready to be boarded, or is already boarded.
     */
    public function getMerchantInventory(): ?bool
    {
        return $this->merchantInventory;
    }

    /**
     * Sets Merchant Inventory.
     * Boolean that indicates if the terminal is assigned to the merchant inventory. This is returned when
     * the terminal is assigned to a merchant account.
     *
     * - If **true**, this indicates that the terminal is in the merchant inventory. This also means that
     * the terminal cannot be boarded.
     *
     * - If **false**, this indicates that the terminal is assigned to the merchant account as an in-store
     * terminal. This means that the terminal is ready to be boarded, or is already boarded.
     *
     * @maps merchantInventory
     */
    public function setMerchantInventory(?bool $merchantInventory): void
    {
        $this->merchantInventory = $merchantInventory;
    }

    /**
     * Returns Store.
     * The store code of the store that the terminal is assigned to.
     */
    public function getStore(): ?string
    {
        return $this->store;
    }

    /**
     * Sets Store.
     * The store code of the store that the terminal is assigned to.
     *
     * @maps store
     */
    public function setStore(?string $store): void
    {
        $this->store = $store;
    }

    /**
     * Returns Terminal.
     * The unique terminal ID.
     */
    public function getTerminal(): string
    {
        return $this->terminal;
    }

    /**
     * Sets Terminal.
     * The unique terminal ID.
     *
     * @required
     * @maps terminal
     */
    public function setTerminal(string $terminal): void
    {
        $this->terminal = $terminal;
    }

    /**
     * Converts the FindTerminalResponse object to a human-readable string representation.
     *
     * @return string The string representation of the FindTerminalResponse object.
     */
    public function __toString(): string
    {
        return ApiHelper::stringify(
            'FindTerminalResponse',
            [
                'companyAccount' => $this->companyAccount,
                'merchantAccount' => $this->merchantAccount,
                'merchantInventory' => $this->merchantInventory,
                'store' => $this->store,
                'terminal' => $this->terminal
            ]
        );
    }

    /**
     * Encode this object to JSON
     *
     * @param bool $asArrayWhenEmpty Whether to serialize this model as an array whenever no fields
     *        are set. (default: false)
     *
     * @return array|stdClass
     */
    #[\ReturnTypeWillChange] // @phan-suppress-current-line PhanUndeclaredClassAttribute for (php < 8.1)
    public function jsonSerialize(bool $asArrayWhenEmpty = false)
    {
        $json = [];
        $json['companyAccount']        = $this->companyAccount;
        if (isset($this->merchantAccount)) {
            $json['merchantAccount']   = $this->merchantAccount;
        }
        if (isset($this->merchantInventory)) {
            $json['merchantInventory'] = $this->merchantInventory;
        }
        if (isset($this->store)) {
            $json['store']             = $this->store;
        }
        $json['terminal']              = $this->terminal;

        return (!$asArrayWhenEmpty && empty($json)) ? new stdClass() : $json;
    }
}
