<?php

declare(strict_types=1);

/*
 * Adyen
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

namespace AdyenLib\Models;

use AdyenLib\ApiHelper;
use stdClass;

/**
 * Contains the card user's password and mobile phone number. This is required when you issue cards
 * that can be used to make online payments within the EEA and the UK, or can be added to digital
 * wallets. Refer to [3D Secure and digital wallets](https://docs.adyen.com/issuing/3d-secure-and-
 * wallets) for more information.
 */
class Authentication1 implements \JsonSerializable
{
    /**
     * @var string|null
     */
    private $email;

    /**
     * @var string|null
     */
    private $password;

    /**
     * @var Phone11|null
     */
    private $phone;

    /**
     * Returns Email.
     * The email address where the one-time password (OTP) is sent.
     */
    public function getEmail(): ?string
    {
        return $this->email;
    }

    /**
     * Sets Email.
     * The email address where the one-time password (OTP) is sent.
     *
     * @maps email
     */
    public function setEmail(?string $email): void
    {
        $this->email = $email;
    }

    /**
     * Returns Password.
     * The password used for 3D Secure password-based authentication. The value must be between 1 to 30
     * characters and must only contain the following supported characters.
     *
     * * Characters between **a-z**, **A-Z**, and **0-9**
     *
     * * Special characters: **äöüßÄÖÜ+-* /ç%()=?!~#'",;:$&àùòâôûáúó**
     */
    public function getPassword(): ?string
    {
        return $this->password;
    }

    /**
     * Sets Password.
     * The password used for 3D Secure password-based authentication. The value must be between 1 to 30
     * characters and must only contain the following supported characters.
     *
     * * Characters between **a-z**, **A-Z**, and **0-9**
     *
     * * Special characters: **äöüßÄÖÜ+-* /ç%()=?!~#'",;:$&àùòâôûáúó**
     *
     * @maps password
     */
    public function setPassword(?string $password): void
    {
        $this->password = $password;
    }

    /**
     * Returns Phone.
     * The phone number where the one-time password (OTP) is sent.
     *
     * This object must have:
     *
     * * A `type` set to **mobile**.
     *
     * * A `number` with a valid country code.
     *
     * * A `number` with more than 4 digits, excluding the country code.
     *
     * >Make sure to verify that the card user owns the phone number.
     */
    public function getPhone(): ?Phone11
    {
        return $this->phone;
    }

    /**
     * Sets Phone.
     * The phone number where the one-time password (OTP) is sent.
     *
     * This object must have:
     *
     * * A `type` set to **mobile**.
     *
     * * A `number` with a valid country code.
     *
     * * A `number` with more than 4 digits, excluding the country code.
     *
     * >Make sure to verify that the card user owns the phone number.
     *
     * @maps phone
     */
    public function setPhone(?Phone11 $phone): void
    {
        $this->phone = $phone;
    }

    /**
     * Converts the Authentication1 object to a human-readable string representation.
     *
     * @return string The string representation of the Authentication1 object.
     */
    public function __toString(): string
    {
        return ApiHelper::stringify(
            'Authentication1',
            ['email' => $this->email, 'password' => $this->password, 'phone' => $this->phone]
        );
    }

    /**
     * Encode this object to JSON
     *
     * @param bool $asArrayWhenEmpty Whether to serialize this model as an array whenever no fields
     *        are set. (default: false)
     *
     * @return array|stdClass
     */
    #[\ReturnTypeWillChange] // @phan-suppress-current-line PhanUndeclaredClassAttribute for (php < 8.1)
    public function jsonSerialize(bool $asArrayWhenEmpty = false)
    {
        $json = [];
        if (isset($this->email)) {
            $json['email']    = $this->email;
        }
        if (isset($this->password)) {
            $json['password'] = $this->password;
        }
        if (isset($this->phone)) {
            $json['phone']    = $this->phone;
        }

        return (!$asArrayWhenEmpty && empty($json)) ? new stdClass() : $json;
    }
}
